{
  "name": "asm-lens",
  "displayName": "ASM Lens",
  "description": "View disassembly side-by-side with C/C++ source code",
  "version": "0.1.0",
  "publisher": "asm-lens",
  "license": "MIT",
  "engines": {
    "vscode": "^1.85.0"
  },
  "extensionKind": [
    "workspace"
  ],
  "categories": [
    "Programming Languages",
    "Debuggers"
  ],
  "activationEvents": [
    "workspaceContains:.asm-lens.json"
  ],
  "main": "./bin/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "asm-lens.showAssembly",
        "title": "ASM Lens: Show Assembly"
      },
      {
        "command": "asm-lens.refresh",
        "title": "ASM Lens: Refresh Disassembly"
      },
      {
        "command": "asm-lens.initConfig",
        "title": "ASM Lens: Initialize Config"
      }
    ],
    "configuration": {
      "title": "ASM Lens",
      "properties": {}
    }
  },
  "scripts": {
    "compile": "esbuild src/extension.ts --bundle --outdir=bin --external:vscode --format=cjs --platform=node",
    "watch": "esbuild src/extension.ts --bundle --outdir=bin --external:vscode --format=cjs --platform=node --watch",
    "package": "vsce package"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "@types/vscode": "^1.85.0",
    "@vscode/vsce": "^3.7.1",
    "esbuild": "^0.20.0",
    "typescript": "^5.3.0"
  }
}
